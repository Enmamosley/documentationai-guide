---
title: Base de datos
description: Esquema Supabase, tablas, triggers, helpers.
---

## Proyecto

- **Proveedor**: Supabase
- **Project ID**: `lvqqzpabnmdkamxgxrob`
- **Región**: `us-east-2`
- **Postgres**: 17
- **API**: PostgREST autogenerado + Row-Level Security

## Tablas

| Tabla | PK | Filas iniciales | Notas |
|---|---|---|---|
| `tecnicos` | text (`T-014`, …) | 6 | Padrón de técnicos |
| `tecnicos_expediente` | tecnico_id | 6 | PII (RFC/CURP/IMSS/INE) + `foto_url` |
| `almacenes` | text (`ALM-VC`, …) | 3 | Sedes físicas |
| `skus` | text (clave AXAPTA) | 46 | Incluye `siatel`, `factor_alta_ftth`, `factor_mig_ftth`, `factor_mig_ftth_vieja`. **34 reales** + 12 mock históricos |
| `os` | uuid | 86 | Estado enum, FK a `tecnicos` |
| `os_materiales` | uuid | varía | Partidas por OS (heredan RLS de la OS padre) |
| `onts` | text (alfanumérico) | 120 | FK a `almacenes`, `tecnicos`, `os` |
| `inventory` | (sku, almacen_id OR tecnico_id) | 63 | XOR: una fila es de almacén **o** mochila. UNIQUE `(sku, COALESCE(almacen_id,''), COALESCE(tecnico_id,''))` |
| `facturas_emitidas` | uuid | 28 | Enum `factura_emitida_estado` de 10 estados (WCG → CARSO) |
| `remisiones` | uuid | varía | Estado `Recibida` → `Confirmada` (default `Recibida`) |
| `remision_items` | uuid | — | UNIQUE `(remision_id, sku)` (Bug 10) |
| `devoluciones` · `devolucion_items` | uuid | — | UNIQUE `(parent_id, sku)` |
| `traspasos` · `traspaso_items` | uuid | — | Movimientos entre almacenes WCG (`traspaso_estado` con `En tránsito` válido) |
| `salidas_tecnico` · `salida_tecnico_items` | uuid | — | UNIQUE `(parent_id, sku)` (kit semanal) |
| `emu_rows` | uuid | varía | Cobranza semanal técnico × concepto |
| `documentos` | uuid | varía | Repo central; opcional `tecnico_id` u `os_id` para scope |
| `programas_obra` · `programa_dias` | uuid | varía | Gantt semanal CARSO |
| `user_profiles` | uuid (FK `auth.users`) | 1+ | Rol enum (admin/almacenista/operaciones/contable/tecnico), `tecnico_id` opcional |

<Callout kind="info">
  La tabla legacy `facturas` (enum `factura_estado`) y el estado `En tránsito` del enum `remision_estado` fueron eliminados en el commit `60248f3` (Bugs 9 y 13). Ya no aparecen en `database.types.ts`.
</Callout>

## Enums

```sql
os_estado: 'Capturada' | 'Liquidada' | 'Facturada' | 'Pagada' | 'Cancelada'
ont_estado: 'En almacén' | 'Con técnico' | 'Liquidada' | 'Facturada' | 'Dañada'
factura_emitida_estado:
  'Borrador' | 'Emitida' | 'Enviada al lote' | 'En revisión CARSO' |
  'Aceptada' | 'Rechazada' | 'Por pagar' | 'Pagada parcial' |
  'Pagada' | 'Cancelada'
remision_estado: 'Recibida' | 'Confirmada'              -- default 'Recibida'
traspaso_estado: 'Borrador' | 'En tránsito' | 'Recibido' -- 'En tránsito' SÍ es válido acá
tipo_telmex: 'alta_ftth' | 'migracion_ftth' | 'migracion_ftth_vieja' | 'no_aplica'
user_role: 'admin' | 'almacenista' | 'operaciones' | 'contable' | 'tecnico'
estado_cobro: 'pendiente' | 'facturado' | 'cobrado' | 'rechazado'
```

## RLS (Row-Level Security)

<Callout kind="alert" collapsed="false">
  **RLS está ACTIVO en todas las tablas.** Sin sesión válida, todas las queries fallan con `42501`. No se confía en el cliente — la seguridad vive en Postgres.
</Callout>

### Lectura

<Callout kind="alert" collapsed="false">
  **Cambio importante (commit `d141f9f`, parche D).** Antes de este commit, casi todas las tablas tenían una policy uniforme `USING (true)`, lo que permitía que un técnico leyera expedientes ajenos, facturas, etc. vía `supabase-client` desde devtools. Ahora las SELECT policies están endurecidas por rol y ownership.
</Callout>

| Tabla | Quién puede SELECT |
|---|---|
| `tecnicos_expediente` | admin/operaciones/contable todos · técnico **sólo el suyo** |
| `facturas_emitidas` | admin/contable |
| `documentos` | operativos todos · técnico **sólo los vinculados a él o a una OS suya** |
| `os` · `onts` · `emu_rows` · `inventory` | admin/operaciones/almacenista (+contable según) · técnico **filtrado por su `tecnico_id`** |
| `os_materiales` · `*_items` | heredado del parent table vía subquery |
| `remisiones` · `devoluciones` · `traspasos` · `salidas_tecnico` | operativos · técnico ve las suyas cuando aplica |
| `tecnicos_herramienta` · `tecnicos_vehiculo` | admin/operaciones/contable · técnico **la suya** |
| `user_profiles` | admin todos · resto **sólo su propio profile** |
| `programas_obra` · `programa_dias` | admin/operaciones |
| Catálogos puros (`skus`, `almacenes`, `tecnicos`, `copes`, `tipos_tarea`, `empresas_empleadoras`, `emu_conceptos`, `unidades_medida`, `clientes`, `empresa_config`) | `to authenticated using (true)` — son data de referencia sin la cual nada funciona |

Para ver "asignado a X" desde un contexto distinto a `/sistema`, usar el RPC `list_users_for_admin` en vez de leer `user_profiles` directo.

### Mutaciones

Cada tabla tiene policies por rol via `current_user_role()`:

| Tabla | INSERT | UPDATE |
|---|---|---|
| `os` | admin · operaciones · tecnico (suya) | admin · operaciones · tecnico (suya) |
| `onts` | admin · almacenista | admin · operaciones · almacenista · tecnico (asignada — `liquidarONTAction` valida ownership de la OS, Bug 11) |
| `facturas_emitidas` | admin · contable | admin · contable |
| `inventory` | admin · almacenista | admin · almacenista (vía triggers atómicos) |
| `remisiones` | admin · almacenista · operaciones | admin · almacenista |
| `*_items` (remision/devolucion/traspaso/salida_tecnico) | mismo rol que el parent · UNIQUE `(parent_id, sku)` (Bug 10) | igual |
| `emu_rows` | trigger SQL (auto al liquidar OS) | admin · contable |
| `user_profiles` | admin | admin |

### Helper SQL

```sql
CREATE FUNCTION current_user_role() RETURNS user_role AS $$
  SELECT rol FROM user_profiles WHERE id = auth.uid()
$$ LANGUAGE sql STABLE;
```

## Atomicidad — RPCs + 1 trigger

WCG App usa **RPCs `SECURITY DEFINER` llamadas explícitamente** desde las server actions para los movimientos críticos. Sólo un caso es trigger automático.

<Steps>
  <Step title="RPC liquidar_os_atomica → consume mochila" icon="package-minus" title-type="p">
    Cuando el técnico (o ops/admin) liquida la OS, `liquidarOSAction` llama esta RPC. Para cada partida con `cantidad_consumida IS NULL`, setea consumida=entregada y descuenta el delta de `inventory` (mochila técnico) vía upsert atómico `INSERT…ON CONFLICT DO UPDATE`. Si la mochila no alcanza, queda negativa (descuadre visible, no error — el técnico debe conciliar via devolución).
  </Step>

  <Step title="RPC confirmar_remision → incrementa almacén" icon="package-plus" title-type="p">
    Al confirmar una remisión, `confirmarRemisionAction` llama esta RPC. Lee `stock_cargado_at`: si es NULL, incrementa `disponible` del almacén destino (upsert atómico) y sella el flag; si ya estaba sellado, sólo avanza estado a `Confirmada` (idempotente).
  </Step>

  <Step title="TRIGGER emu_avanzar_cobrado → único trigger del sistema" icon="check-circle" title-type="p">
    `AFTER UPDATE` en `facturas_emitidas`. Cuando `estado` pasa a `Pagada` **completa** (no 'Pagada parcial' — Bug 6), todas las `emu_rows.factura_emitida_id` asociadas pasan a `estado_cobro='cobrado'` y se sella `fecha_cobrado`.
  </Step>

  <Step title="Protección de escalation por RLS WITH CHECK" icon="shield" title-type="p">
    No hay trigger `prevent_role_escalation`. La protección de Bug 4 vive en la RLS policy `Users update own profile (no rol/tecnico_id)` con `WITH CHECK` que exige `rol = (current rol del user) AND tecnico_id = (current tecnico_id)`. Si un técnico intenta `update({rol:'admin'})` se rechaza con `42501`. Sólo el admin puede cambiar roles vía RPC `cambiar_rol_usuario`.
  </Step>
</Steps>

<Callout kind="info">
  Otros RPCs atómicos relevantes en stock: `recibir_remision`, `surtir_a_tecnico` (con guard de stock insuficiente), `ejecutar_traspaso` (idem), `devolver_al_almacen`, `reportar_consumo_atomico`, `agregar_partida_os`. Todos siguen el patrón `INSERT…ON CONFLICT DO UPDATE` sobre `inventory` para evitar las filas duplicadas que tenía la versión anterior (Bug 1).
</Callout>

## Tres clientes Supabase

```typescript
// src/lib/supabase-server.ts
getSupabaseServer()        // Server Components, Server Actions, Route Handlers
                           // Cookies → JWT → RLS evalúa el rol

// src/lib/supabase-client.ts
supabase                    // Client Components (raramente; preferir actions)

// middleware.ts
createServerClient(...)     // Refresh de tokens en cada request
```

## Re-seed reproducible

```bash
# Semillas RNG fijas → mismos datos que el prototipo original
node scripts/build-seed.mjs > scripts/seed.sql
node scripts/build-telmex-import.mjs > scripts/telmex-import.sql

# Aplicar con tu cliente psql preferido contra el proyecto Supabase
psql $SUPABASE_DB_URL -f scripts/seed.sql
psql $SUPABASE_DB_URL -f scripts/telmex-import.sql
```

## Regenerar tipos

```bash
npx supabase gen types typescript \
  --project-id lvqqzpabnmdkamxgxrob \
  > src/lib/database.types.ts
```
